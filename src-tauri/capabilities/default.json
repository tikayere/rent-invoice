{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions granted to the main application window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
